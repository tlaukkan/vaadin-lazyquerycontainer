/**
 * Lazy Query Container is a Vaadin framework addon which provides lazy loading data container 
 * implementation for Vaadin tables. Lazy Query Container is read only container and data
 * modifications are designed to be done through a business delegate. The QueryFactory
 * and Query interfaces are designed to be application specific and should apply the business
 * delegate for querying data from business services.
 */
package org.vaadin.addons.lazyquerycontainer;